package steps;

import baseEntities.BaseStep;
import org.openqa.selenium.WebDriver;

public class NewWindowStep extends BaseStep {
    public NewWindowStep(WebDriver driver) {
        super(driver);
    }
}
